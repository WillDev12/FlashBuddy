Make a quizlet alternative:

Local, no database or 'dashboard,' just decks you can choose from in a dropdown at the top left
   1. You can manage decks, but there aren't public/private decks, accounts, or anything that would be considered 'interweb'
Has studying functionality (match, learn, test, flashcards)
Simple UI, nothing too fancy
You can create flashcard decks or import them
   1. When importing, the user shares a quizlet deck link
   2. The quizlet deck is web scraped for content and it is processed into the local program
Decks are stored in cookies with the option to manually save them as files so they don't get lost
   1. Should be able to import these files if chosen
Should be able to be built into a standalone .html file

New Changes:

1. Completely remove railway server -- docker image, etc... anything involved with railway.  I'll delete the project on the website.
   1. Vercel server will be kept up for download page, but scraper functionality will be returned to the local machine
2. Re-organize releases so that they have a standalone (url import disabled), and a scraper-included (downloads the scraper server too with simple startup scripts so users can scrape from their local devices)
   1. Add this change to the build script so that both of these are built
   2. Scraper-included should be bundled in a zip file
   3. A download link for scraper-included should be included on /download on the actual server
3. Add another import method
   1. User instead hits 'save as...' on the Quizlet page to download it to their local device
   2. User uploads it to the program
   3. The program scrapes the local html to derive terms (no longer a need for puppeteer!)
4. Add a ./docs folder to the root that has .md tutorials on the following
   1. How to use each import function
   2. How to set up the server in scraper-included
5. Add a /docs to the actual server which reflects the markdown files in ./docs
